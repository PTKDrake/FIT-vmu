/**
 * Simple translation helper for internationalization.
 * In the future, this can be integrated with actual i18n libraries like i18next.
 */
export function t(text: string): string {
  return text;
}
